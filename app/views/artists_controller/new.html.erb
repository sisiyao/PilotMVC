<head>
  <style>
    body, h1, h2, div, img, a {
      margin: 0;
      padding: 0;
      border: 0;
      font-family: sans-serif;
    }

    h1 {
      font-size: 40px;
      text-align: center;
      padding: 30px 20px 0 20px;
    }

    .header {
      width: 100%;
      height: 72px;
      margin: 0 auto;
      background-color: #abb3ed;
      text-align: center;
      position: fixed;
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
      color: white;
    }

    .header > div {
      margin: 0 35px;
    }

    .filler {
      height: 85px;
    }

    .button {
      font-size: 20px;
      font-weight: bold;
      color: black;
    }

    .button > a:link, .button > a:visited {
      text-decoration: none;
      color: black;
    }

    .button > a:hover {
      cursor: pointer;
      color: white;
    }

    .form {
      min-width: 400px;
      max-width: 400px;
      margin: 0 auto;
      text-align: center;
    }

    form {
      display: flex;
      flex-direction: column;
    }

    .submit {
      width: 150px;
      font-size: 40px;
    }
  </style>
</head>

<body>
  <div class="header">
    <div class="button"><a href="/">MyMusicApp [built with Ruby on Pilot framework]</a></div>
    <div class="button">New Artist</div>
  </div>

  <div class="filler"></div>
  <h1>New Artist Form</h1>
  <div class="form">
    <form action="/artists" method="POST">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
      <input type="text" name="artist[name]" placeholder="Name" value="<%= @artist.name %>">
      <input type="text" name="artist[image_url]" placeholder="Image URL" value="<%= @artist.image_url %>">
      <input class="submit" type="submit" value="Create New Artist">
    </form>
  </div>

  <div class="errors"><%= flash[:errors].join(", ") if flash[:errors] %></div>
</body>
